var a = 20;

console.log(a);

var greet = 'hello';

console.log(greet);

//js is a dynamically typed language.
// i.e variables can receive different values and types at runtime and their type is defined at runtime

let variable;
variable = true;
console.log(variable);
variable = 2;
console.log(variable);

//variable in js are case sensitive 
// variables in js can begin with $

